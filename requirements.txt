# Core ML & Embeddings
sentence-transformers>=3.0.0,<4.0.0   # Free HuggingFace embeddings (runs on CPU/MPS)
transformers>=4.44.0,<5.0.0           # HuggingFace transformer models
torch>=2.2.0,<3.0.0                   # PyTorch — M-series uses MPS backend automatically

# LangChain Ecosystem
langchain>=0.3.0,<0.4.0                # Main orchestration framework
langchain-community>=0.3.0,<0.4.0      # Community integrations (FAISS, HuggingFace, etc.)
langchain-huggingface>=0.1.0,<0.2.0    # LangChain ↔ HuggingFace embeddings bridge
langgraph>=0.2.0,<0.3.0                # Multi-agent graph orchestration
langsmith>=0.1.0,<0.3.0                # Tracing and evaluation for LangChain apps

# Vector Store
faiss-cpu>=1.8.0,<2.0.0                # Fast local vector search (no GPU needed)

# PDF Processing
pymupdf>=1.24.0,<2.0.0                 # Fast PDF text + page metadata extraction

# LLM (Free, Local)
llama-cpp-python>=0.2.80,<0.4.0        # Run GGUF quantised LLMs locally on CPU/Metal
huggingface-hub>=0.24.0,<1.0.0         # Download models from HuggingFace Hub

# Data Handling
pandas>=2.2.0,<3.0.0                   # DataFrame operations for JSONL dataset
numpy>=1.26.0,<3.0.0                   # Numerical ops

# Web Server
fastapi>=0.110.0,<1.0.0                # REST + SSE API for the React frontend
uvicorn[standard]>=0.28.0,<1.0.0       # ASGI server (used by FastAPI)

# Evaluation
rouge-score>=0.1.2,<0.2.0              # ROUGE metric for answer quality
rank-bm25>=0.2.2,<0.3.0                # BM25 keyword retrieval

# Utilities
python-dotenv>=1.0.0,<2.0.0            # Load env variables from .env
tqdm>=4.66.0,<5.0.0                    # Progress bars
pydantic>=2.7.0,<3.0.0                 # Data validation / typed state
